# Allow the planner namespace to reach the Model Catalog API
# in rhoai-model-registries.  Applied separately because the target
# namespace is outside of planner.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-planner-to-model-catalog
  namespace: rhoai-model-registries
  labels:
    app.kubernetes.io/part-of: planner
spec:
  # Match the Model Catalog pods.  Use a single label to avoid
  # fragility if the upstream deployment labels change.
  podSelector:
    matchLabels:
      app.kubernetes.io/name: model-catalog
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: planner
          podSelector:
            matchLabels:
              app.kubernetes.io/name: backend
      ports:
        - port: 8443
          protocol: TCP
  policyTypes:
    - Ingress
